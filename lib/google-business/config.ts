/** OAuth redirect path - register in Google Cloud > Credentials (same Web client as Gmail). */
export const GBP_OAUTH_CALLBACK_PATH = '/api/integrations/google-business/callback';

export { requireGoogleOAuthClient } from '@/lib/gmail/config';
